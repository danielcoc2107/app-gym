# T00 · Base web, pruebas y CI

Validación local inicial: 2026-09-07. Publicación GitHub: 2026-09-08.
Verificación del despliegue: 2026-09-10. QA remota completada: 2026-09-11 (America/Bogota).
Rama: `codex/t00-base-ci`. Implementada, con CI correcta y preview verificado.
Preparada para revisión del usuario; conformidad y merge todavía pendientes.

## Alcance implementado

- Next.js App Router, React, TypeScript estricto y Tailwind CSS, con Node.js 24.
- Bienvenida responsive, explicación del producto y página 404 con retorno al inicio.
- Navegación real, foco visible, enlace de salto, listas y secciones accesibles.
- Vitest/Testing Library, Playwright de producción, lockfile y workflow de GitHub Actions.
- README de instalación aislada, pruebas, conexión Vercel y futura preparación de Supabase.

Las pantallas indican qué funciones llegarán después. No crean cuentas, inventarios,
rutinas ni sesiones. No se ha modificado autenticación ni ningún archivo de entorno real.
No se han creado módulos vacíos, migraciones, servicios de Supabase ni claves.
Se creó y conectó el proyecto Vercel autorizado; no se contrataron servicios de pago.
La solución de inventario personal provisional está especificada, aún no implementada.

## Validación local observada

Repetida el 2026-09-11: lint, unitarias, build y E2E correctos, sin cambios de fuente.

| Comprobación | Resultado |
| --- | --- |
| `npm run lint` | Correcto, cero advertencias |
| `npm run test` | 9 pruebas, 5 archivos, todas correctas |
| Cobertura V8 | 100% de sentencias, funciones y líneas instrumentadas; no hay ramas todavía |
| `npm run build` | Compilación y comprobación TypeScript correctas; rutas estáticas generadas |
| `npm run test:e2e` | 18 pruebas correctas, Chromium escritorio y emulación móvil |
| `npm audit --json` | 0 vulnerabilidades conocidas en la auditoría inicial de T00; no repetida en la QA remota |
| `npm ci --dry-run --ignore-scripts --no-audit` | Lockfile coherente; simulación correcta, no reinstalación completa |
| `test:db` | No aplicable: T01 incorpora Supabase local y pruebas RLS reales |

La cobertura excluye layout, metadatos separados, declaraciones de tipos y archivos de
pruebas. El cableado del layout se comprueba en navegador. El umbral mantiene la base
medida de T00; no representa cobertura de funcionalidades todavía inexistentes.

Se verificó el camino de fallo con una prueba temporal deliberadamente incorrecta:
la suite devolvió código 1 e identificó la aserción. Se retiró esa sonda y se ejecutó
la suite completa nuevamente con éxito, sin modificar ni omitir pruebas existentes.

Los E2E verifican navegación, respuesta 404, ausencia de desbordamiento a 360 px,
CTA por teclado, enlace de salto y semántica de listas. Los recorridos de navegación
también detectan errores de consola y excepciones; solo permiten el 404 esperado de
la ruta inexistente consultada expresamente. No equivalen a pruebas reales de Safari.

## Revisión manual

Iniciar con `npm run dev` y abrir [App Gym local](http://127.0.0.1:3000).
Para revisar exactamente la compilación, usar `npm run build` y `npm run start`.
El servidor de E2E usa otro puerto, el 3100, y se cierra al terminar la suite.

1. Abrir inicio en escritorio y móvil: textos legibles y sin desplazamiento horizontal.
2. Pulsar «Conoce cómo funciona» y «Volver al inicio».
3. Usar Tab y Enter para comprobar «Saltar al contenido» y los enlaces.
4. Visitar una ruta inexistente y volver desde la página 404.
5. Confirmar que se anuncian funciones futuras y no se simulan datos guardados.

En local se inspeccionaron inicio y «Cómo funciona» en escritorio (1365 × 900) y móvil
(390 × 844), con navegación de ida y vuelta y sin avisos/errores de consola observados.
El tamaño del navegador se restauró al finalizar la revisión.

### Verificación del preview

Se revisó el enlace estable de `codex/t00-base-ci` con acceso autorizado a Vercel,
conectando el PC al punto de acceso móvil. La referencia desplegada al iniciar esta
revisión era `922793f`; no se modificó código para resolver el acceso.

- Inicio y «Cómo funciona» inspeccionados en escritorio (1365 × 900) y móvil
  (390 × 844 y 360 × 800), con textos legibles y sin desbordamiento horizontal.
- Navegación de ida y vuelta mediante los enlaces reales de ambas pantallas.
- «Saltar al contenido» recibió foco con Tab y, con Enter, lo trasladó a `#contenido`.
- Página 404 comprobada visitando `/qa-ruta-inexistente`, con retorno al inicio.
- Documento en español y metadatos `noindex, nofollow` observados; listas de la
  explicación con cuatro pasos. No se simulan cuentas ni entrenamientos guardados.
- No se observaron avisos ni errores nuevos de consola durante el recorrido remoto.
  Se restauró el tamaño del navegador al finalizar. La emulación no equivale a Safari real.

El usuario también aportó una captura del inicio cargado en su teléfono con datos
móviles. Eso confirma acceso e inicio, no constituye todavía aprobación funcional del PR.

## Publicación y pendientes para cerrar T00

- [PR 1: especificación](https://github.com/danielcoc2107/app-gym/pull/1), abierto contra `main`.
- [PR 2: T00](https://github.com/danielcoc2107/app-gym/pull/2), contra la rama de especificación.
- [CI de la referencia revisada](https://github.com/danielcoc2107/app-gym/actions/runs/34560892994):
  correcta para `922793f`, con lint, unitarias, build y E2E. La descripción del PR
  registra los checks del último commit, incluidos los posteriores de documentación.
- [Proyecto Vercel](https://vercel.com/coc12/app-gym): conectado a `danielcoc2107/app-gym`,
  Next.js, Node 24, `npm ci` y `npm run build`, sin variables ni base de datos.
- [Preview generado automáticamente](https://vercel.com/coc12/app-gym/BKdUk7KpxoJaE1pyHmxE3jYsnHS6):
  entorno `Preview`, fuente `codex/t00-base-ci` / `922793f`, compilación `Ready` en 29 s.
  [URL de esa compilación](https://app-pju803v4t-coc12.vercel.app/) y
  [enlace estable de la rama](https://app-gym-git-codex-t00-base-ci-coc12.vercel.app/).
- Incidencia de acceso: las URL devolvieron `403 Forbidden` en el navegador integrado,
  también al entrar mediante «Visit» del panel. Una petición HTTP sin sesión redirige
  al acceso de Vercel. Se comprobó `Require Log In` activo con `Standard Protection`;
  esa configuración se mantuvo sin cambios.
- El usuario confirmó el mismo error al abrir el enlace de la rama. El dominio público
  del despliegue inicial respondió HTTP 200 y HTML de App Gym a una petición sin sesión,
  pero también mostró 403 en el navegador integrado. Los registros remotos muestran
  respuestas 200 en `/` y `/como-funciona`; no equivalen a validar las visitas fallidas.
- El panel Firewall mostró cero reglas personalizadas, protección de bots inactiva,
  sin IP denegadas ni acciones persistentes en el intervalo consultado. No identificó
  una regla concreta responsable del 403; no se atribuye el fallo a autenticación,
  firewall o código sin más evidencia.
- El usuario comprobó que el mismo teléfono abre la app con datos móviles y devuelve
  403 con el Wi-Fi original. Tras conectar el PC al hotspot del teléfono, el preview
  también cargó en el navegador integrado y se completó la QA remota descrita arriba.
  La evidencia apunta a una diferencia de red/acceso, sin identificar la causa exacta.
  El uso del hotspot permite revisar, pero no es una reparación del Wi-Fi original.
- No se desactivó la protección, no se generaron secretos de bypass ni se cambiaron roles.
- La verificación técnica permite entregar el PR para revisión; falta conformidad
  del usuario antes de fusionar. No se ha hecho merge ni iniciado T01.

### Particularidad del primer despliegue

La primera solicitud se realizó con el botón «Create Preview Deployment» desde la rama
T00, pero Vercel la clasificó como `Production`: [despliegue inicial](https://vercel.com/coc12/app-gym/HEjr4Cd3e4KgvXtuhiCNjfD5YfyJ),
`7836f28`, estado `Ready`. Es el [comportamiento documentado del primer despliegue](https://vercel.com/docs/deployments/environments#first-deployment).
Posteriormente se creó el preview separado indicado arriba; no se promovió ese preview.
La rama de producción configurada continúa siendo `main`; su commit remoto comprobado
es `c17cd79be5bd68b227adc8b225174e66f2b27b19`, sin cambios. El rótulo `Production`
del despliegue inicial no significa que T00 haya sido aprobada ni que el MVP esté terminado.

El usuario autorizó publicar las ramas, abrir PR y conectar Vercel el 2026-09-07.
La especificación se entrega por separado en `codex/especificacion-entrenamiento`
contra `main`; T00 se presenta inicialmente contra esa rama para mantener su diff
limitado a infraestructura. Revisar primero la especificación y después T00.
Esta autorización no incluye fusionar ni configurar Supabase/Auth.

T01 requiere además permiso explícito para autenticación y configuración del entorno;
esta entrega no lo presupone. Ver [ficha T01](../tareas/base.md#t01-autenticacion-inicial).
